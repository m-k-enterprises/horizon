# Assets

Static files (CSS, JS, and images) for theme templates.

Rules: [Assets](../.cursor/rules/assets.mdc)

Keep changes scoped to theme template static files unless a closely related dependency must also be updated. Use the referenced Assets rules as the source of truth. After making changes, perform a lightweight verification that asset paths, references, and related static-file behavior remain consistent.